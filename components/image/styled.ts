import { styled } from '../../stitches.config';

export const StyledImageContainer = styled('div', {
  overflow: 'hidden',
  borderRadius: '10px',
  position: 'relative',
  margin: '10px 0',
});

export const StyledExternalImageContainer = styled('div', {
  margin: '50px 0',
});
